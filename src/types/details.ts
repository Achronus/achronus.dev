export type AboutDetailsType = {
  intro: string;
  firstName: string;
  lastName: string;
  title: string;
  desc: string;
  image: string;
};

export type FeaturedCardType = {
  title: string;
  desc: string;
  iconStyles: string;
  icon: React.ReactElement;
};
